services:
  api:
    build:
      context: .
      target: api
    container_name: island-bitcoin-api
    ports:
      - "${API_PORT:-3001}:3001"
    volumes:
      - sqlite-data:/data
    environment:
      - NODE_ENV=production
      - DATABASE_PATH=/data/island-bitcoin.db
      - PORT=3001
      - ORY_TOKEN=${ORY_TOKEN:-}
      - AUTO_APPROVE=${AUTO_APPROVE:-false}
      # Comma-separated Evento.so event IDs to pull into the events page.
      # Add new events here as Island Bitcoin creates them on evento.so.
      # Example: evt_zRt3yEMyk1zuud55,evt_abc123,evt_def456
      - EVENTO_EVENT_IDS=${EVENTO_EVENT_IDS:-evt_zRt3yEMyk1zuud55}
      - AUTO_APPROVE_THRESHOLD=${AUTO_APPROVE_THRESHOLD:-1000}
    restart: unless-stopped
    networks:
      - island-bitcoin-network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3001/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s

  web:
    build:
      context: .
      target: web
    container_name: island-bitcoin-web
    ports:
      - "${WEB_PORT:-8080}:80"
    depends_on:
      api:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - island-bitcoin-network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:80"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s

networks:
  island-bitcoin-network:
    driver: bridge

volumes:
  sqlite-data:
    driver: local
